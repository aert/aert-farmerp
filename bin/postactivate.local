#!/bin/bash
# This hook is run after this virtualenv is activated.

export DJANGO_SETTINGS_MODULE="web.settings.local"
export ANSIBLE_HOSTS=$PROJECT_HOME/`basename $VIRTUAL_ENV`/deploy/ansible/hosts/vagrant
